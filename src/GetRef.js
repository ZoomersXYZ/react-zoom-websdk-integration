import React, { useEffect, useState } from 'react';
import PropTypes from 'prop-types';

const GetRef = () => {
  useEffect( () => { 
    const zoomMeeting = document.getElementById( 'wc-container-right' );
  }, [] );
  return(
    <>

    </>
  ) 
};

export default GetRef;
